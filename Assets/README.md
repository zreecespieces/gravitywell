# Assets

Launch and marketing assets for GravityWell.

- `screenshots/` contains release screenshots used by the README and website.
- `gifs/` contains the short demo GIF used near the top of the README.
- `logo/` contains logo exports and social-card assets.

The app bundle assets live separately in `GravityWell/Assets/`.
